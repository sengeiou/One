<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ubt.loginmodule">

    <application
        android:name=".LoginApplication"
        android:allowBackup="true"
        android:icon="@drawable/login_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/login_ic_launcher_round"
        android:supportsRtl="true"
        >
        <activity
            android:name="com.ubt.loginmodule.login.LoginActivity"
            android:screenOrientation="landscape"
            android:theme="@style/NoTitleTheme"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.ubt.loginmodule.loginauth.LoginAuthActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="LoginAuthActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/NoTitleThemeFullScreen"
            android:windowSoftInputMode="adjustPan"/>

        <activity
            android:name="com.ubt.loginmodule.useredit.UserEditActivity"
            android:configChanges="orientation|keyboardHidden|navigation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@style/NoTitleThemeFullScreen"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>